version: "1"
successors:
  s3://quilt-ernest-staging:
    title: Test WRITE_BUCKET 
is_workflow_required: False
# default_workflow: my-workflow 
workflows:
  my-workflow:
    name: My Test Workflow
    metadata_schema: my-workflow
schemas:
  my-workflow:
    url: s3://quilt-ernest-staging/.quilt/workflows/my-workflow.json
